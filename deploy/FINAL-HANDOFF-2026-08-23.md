# 8 月 23 日 APK 交付前人工操作清单

当前 ECS：`8.217.147.235`（中国香港，Ubuntu 22.04，2 核 4 GiB）。后端、H5、官方行情采集、价格预测、账号、作物、销路、营销、图片上传、天气、蓝心文本/图片/语音能力和 App 编译资源均已部署并验证。

## 一、域名与 HTTPS（已完成，无需人工操作）

`ysngj.cn` 已解析到 `8.217.147.235`，APK 使用的 `https://ysngj.cn/api` 已通过完整业务回归。Let’s Encrypt 证书有效期至 2026-11-16，Certbot 定时续期和续期后 Nginx 自动重载均已配置并通过演练。HTTP 请求会统一跳转到 HTTPS。

## 二、阿里云账号持有人确认试用实例状态（必须人工）

1. 在阿里云费用中心确认 ECS 免费抵扣额度仍足以覆盖到 8 月 23 日，账号不存在欠费或停机提醒。
2. 确认实例没有设置 8 月 23 日之前的自动释放时间。
3. 答辩前不要“停止”“释放”或更换公网 IP；当前 DNS 和 APK 都将依赖 `8.217.147.235`。
4. 8 月 22 日和答辩当天各检查一次实例状态为“运行中”。

## 三、确认打包身份与签名（必须由账号或证书持有人操作）

1. 使用有权管理 DCloud AppID `__UNI__A0763C1` 的账号登录最新版 HBuilderX。
2. 如果当前 DCloud 账号无权使用该 AppID，需要在 DCloud 控制台完成转移/授权，或创建新 AppID 并同步修改 `src/manifest.json`；这一步必须在正式签名前确定。
3. Android 包名已配置为 `cn.ysngj.agricloud`，不要在首次发布后随意更改。
4. 准备队伍自己持有的 Android 签名证书，保存好 `.keystore`、证书别名和密码。后续升级必须继续使用同一证书。
5. 不要把证书、密码、蓝心 AppKEY 或服务器 `.env` 放进项目压缩包。

## 四、生成正式 APK（必须在 HBuilderX 中人工完成）

1. 解压最新的 `AgriCloudManager-APK-Handoff.zip`。
2. 在项目目录执行 `npm ci`，再执行 `npm run build:app`；成功标志为 `DONE Build complete`。
3. 用 HBuilderX 打开解压后的 `AgriCloudManager2.0` 项目。
4. 选择“发行 → 原生 App-云打包”。
5. 只选择 Android；答辩安装包可选择 `vivo` 渠道。
6. 选择正式包/Release，使用自己的证书；不要选择标准运行基座、自定义调试基座或公共测试证书。
7. 可在支持的 HBuilderX 版本中开启“安心打包”和“防重签”。
8. 下载生成的 `.apk`，记录文件名、版本 `1.6.10`、版本号 `1610`、SHA-256 和签名证书指纹。

## 五、vivo 真机验收（必须人工接触目标手机）

1. 在目标 vivo 手机上卸载旧版本，再安装最终 APK。
2. 首次使用拍照、语音和天气定位时，分别允许相机、麦克风和定位权限；无需提前一次性授予。
3. 使用项目负责人通过安全渠道提供的答辩账号登录；账号密码不保存在公开 Git 仓库中。
4. 依次验证：我的地、添加/编辑作物、待售产品、销路匹配与感兴趣商户、三种行情及未来七天预测、AI 问诊、语音输入、语音播报、营销文案与历史记录、个人头像和通知。
5. 杀掉 App 后重新打开，确认登录状态保留；再切换到移动网络复测一次。
6. 拒绝相机/麦克风/定位权限各测试一次，确认 App 给出提示而不是闪退。
7. 确认系统设置中的应用名称、图标、包名和版本正确，再保留该 APK 作为答辩安装包。

## 已完成，不需要再人工配置

- vivo 蓝心 AppID/AppKEY 已只保存在服务器，文本对话、图片诊断、语音识别和语音合成均已验证；密钥未进入前端或 App 产物。
- `ysngj.cn` DNS、HTTPS、HTTP 跳转、安全响应头、证书自动续期及续期后 Nginx 重载均已完成。
- 登录、注册防接管、密码 scrypt 迁移、登录限流、公开注册与短信万能验证码关闭、安全响应头和 CORS 白名单已部署。
- Android 包名为 `cn.ysngj.agricloud`，`minSdkVersion=23`、`targetSdkVersion=35`、仅包含 ARM 架构并关闭调试。
- 前后端生产依赖审计均为 0 个已知漏洞；预测测试 124/124 通过。
- App 编译资源约 15.4 MB，字体和图片均已进入产物，未检出蓝心密钥、JWT 密钥或开发占位文案。
- SQLite 数据库和用户上传文件已配置每日 03:17 自动备份，自动保留最近 7 天。

## 数据与算法口径

- 苹果、大豆、玉米行情由服务器从全国农产品批发市场价格信息系统自动采集。
- 当前真实历史观测不足 62 条时使用 Holt 阻尼趋势冷启动；达到 62 条真实观测后才允许启用 DLinear 与 N-BEATS。插值数据不计入真实观测数量。
- 未来七天预测为辅助参考，不代表已承诺的成交价格；实际交易仍需结合当地询价、品级、运输和损耗。
